name: ci
on: [push]
jobs:
  t:
    name: tt
    runs-on: [ubuntu-latest]
    steps:
      - name: ttt
        run: |
          echo "hello world"
          apt --help
      - name: tttt
        run: |
          echo "2"
